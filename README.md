# 3-Card Poker Client UI

I built a client program using JavaFX that relies on connecting to a localhost server. The client program communicates with the server program to gather information and play the game. It is relatively complete, but I'd like to revisit this at some point to make the code cleaner and/or create a better UI with improved transitions, game flow, and background elements.
